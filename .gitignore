# Build artifacts
/bin/
/dist/
*.exe
*.test
*.out

# Go
/vendor/
.go-build/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Local upstream Luau source checkout used only for porting reference
/.upstream/
# Local upstream Luau official binaries used for differential testing
/.upstream-bin/
# Swarm worktrees -- ephemeral per-agent copies used during multi-agent
# bug-fix runs. See tools/swarm/README.md.
/.swarm/

# luau-bcrunner is a small C++ harness built locally; its binary is not
# committed but the source under tools/luau-bcrunner/ is.
/tools/luau-bcrunner/bcrunner.exe

# Generated golden bytecode (regenerated via tools/sync-upstream)
# tests/golden/*.luac is *committed* so contributors without upstream luau
# can still run round-trip tests. Do NOT ignore.
